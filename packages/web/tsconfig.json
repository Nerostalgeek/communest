{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
      "outDir": "./dist",
      "rootDir": "./src"
      // Add any other web-specific TypeScript options here
    },
    "include": [
      "src/**/*.ts", // Include all TypeScript files in src
      "src/**/*.tsx" // Include all TypeScript JSX files in src
    ],
    "exclude": [
      "vite.config.ts", // Exclude the Vite config file
      "node_modules" // Commonly excluded to avoid type-checking dependencies
    ]
  }
  